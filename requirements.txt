# 업무기억관 — 실행 의존성
# 개발 환경 기준. 정확한 재현이 필요하면 `pip freeze`로 잠금 파일을 따로 관리한다.

PySide6>=6.11
PyMuPDF>=1.28
python-docx>=1.2
python-pptx>=1.0
openpyxl>=3.1
olefile>=0.47
lxml>=6.1
numpy>=2.0
httpx>=0.28

# 개발·시험용 (배포 패키지에는 불필요)
pytest>=9.1
pytest-cov>=7.1
pyinstaller>=6.22
